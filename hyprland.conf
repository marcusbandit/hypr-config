# Global hyprland specific variables
$terminal   = ghostty
$menu       = rofi -show drun
$browser     = zen-browser
$editor      = nvim
$music       = spotify
$video       = mpv
$filemanager = yazi
$working_dir = ~/.config/hypr/

$conf_path = ~/.config/hypr

# Palette first: everything below refers to $sl* variables from it.
source = $conf_path/theme/slate.conf

source = $conf_path/hyprland/env.conf
source = $conf_path/hyprland/general.conf
source = $conf_path/hyprland/keybinds.conf
source = $conf_path/hyprland/rules.conf
source = $conf_path/hyprland/exec.conf

###########################
### MONITOR CONFIGURATION ###
###########################

# Fallback monitor config
monitor =   ,   preferred,  auto,   1

$ultrawide      =   HDMI-A-1
$vertical_side  =   DP-1

# Home monitor setup:
#           Monitor,        Resulution@Hz,  Pos,        Scale,  Rotation, 1-3,  Bitdepth,   n,  VVR,    Color mode
#monitor =   $vertical_side, 1920x1200@60,   0x0,        1,      transform,  1,
#monitor =   $ultrawide,     5120x1440@144,  1200x240,   0.8,      transform,  0,  bitdepth,   8,  vrr,    1 # Default mode
#monitor =   $ultrawide, 5120x1440@144, 1200x240,    1, transform, 0, bitdepth, 10, vrr, 1,    cm, hdr, sdrbrightness, 1.2, sdrsaturation,0.98 #HDR mode
